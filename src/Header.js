import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <header className="text">
        <span className="header-text"> RocketBook</span>
      </header>
    );
  }
}

export default Header;
